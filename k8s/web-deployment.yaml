apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    app: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: mtk21/wordsmith-web:f2b6a3280f1e071ae29ef85f9ccc0159f8b6ea1c
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: WORDS_HOST
              value: api
            - name: WORDS_PORT
              value: "8080"
            - name: WEB_PORT
              value: "8080"
